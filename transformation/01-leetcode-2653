/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var demomap = function (arr, fn) {
  return arr.map((item) => {
    console.log(item);
  });
};

demomap([1, 2, 3]);

var map = function (arr, fn) {
  const res = [];

  arr.forEach((element, idx) => {
    res.push(fn(arr[i], Number(i)));
  });

  return res;
};
